ip a (copiar el nombre del interface de red)
# crear el archivo de configuración:
sudo nano /etc/systemd/network/10-static.network
#Con el contenido que corresponda:
[Match]
Name=enp0s3

[Network]
Address=192.168.1.50/24
Gateway=192.168.1.1
DNS=1.1.1.1 8.8.8.8
# Deshabilito NetworkManager y habilito sistemd-networkd (mejor para servidores):
sudo systemctl stop NetworkManager
sudo systemctl disable NetworkManager
sudo systemctl enable systemd-networkd
sudo systemctl start systemd-networkd
# Reinicio el servicio (en este momento si estas por ssh perderás la conexión y tendrás que conectar a la nueva IP).
